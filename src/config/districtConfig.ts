const config = {
    fields: {
        display: ['countryId', 'stateId', 'districtName', 'districtCode'],
        actions: [
            { create: 'create', reset: 'reset' }
        ]
    }
};

export default config; 